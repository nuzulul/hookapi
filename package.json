{
  "name": "hookapi",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev --env dev",
    "start": "npx wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
